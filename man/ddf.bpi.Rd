% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddf.bpi.R
\name{ddf.bpi}
\alias{ddf.bpi}
\title{Fits a Buckland et al (2010) model with possible dependence in detection for mark-recapture distance
data.}
\usage{
ddf.bpi(
  mrmodel = list(pformula = ~-1 + observer + observer:distance, dformula = ~-1 +
    distance),
  data,
  meta.data = meta.data,
  control = control,
  call = ""
)
}
\arguments{
\item{mrmodel}{list containing pformula (for detection) and dformula (for dependence)}

\item{data}{dataframe containing double observer data (2 records per observation) with fields observer, detected, distance and possibly ch (capture history=10,01 or 11 for pair of observations}

\item{meta.data}{list containing width and other fields not currently used}

\item{control}{list containing method (for optimization), debug (TRUE or FALSE), initial (optional starting values), indepm(TRUE if full independence), PI point independence if TRUE, use.offset and posdep.}
}
\value{
list containing: fitted parameter values (par), AIC, optimx result with avep and NHat added, pformula and dformula.
}
\description{
Fits a Buckland et al (2010) model with possible dependence in detection for mark-recapture distance
data.
}
\examples{
# example code
par=c( 1.517424, 1.663175, -0.28, -0.5, 0.1311178)
W=10
N=1000
set.seed(1083821)
x=sim_data(N,W,par=par)
#create data structure with 2 records for each observation
dd=make.design.data(x)
#fit model
results=ddf(mrmodel=list(pformula=~observer*distance,dformula=~-1+distance),data=dd,method="bpi",meta.data=list(width=W),control=list(method="nlminb",debug=FALSE))
#N hat
Nhat=nrow(x)/results$mod$avep
Nhat
# \%diff
100*(N-Nhat)/N
# unconditonal detection fcts
par(mfrow=c(3,2))
plot(results,1:6,showpoints=FALSE)
# conditional detection functions
par(mfrow=c(1,2))
plot(results,7:8,showlines=FALSE)

}
\seealso{
ll.bpi
}
\author{
Jeff Laake
}
