% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.bpi.r
\name{sim.bpi}
\alias{sim.bpi}
\title{Generate a set of double observer detection histories from population data and bpi model}
\usage{
sim.bpi(
  x,
  par,
  pformula,
  dformula,
  indep = FALSE,
  PI = TRUE,
  debug = FALSE,
  use.offset = FALSE
)
}
\arguments{
\item{x}{population data - 1 row per observer and must contain distance and observer and object fields at a minimum}

\item{par}{model parameter values}

\item{pformula}{formula for true detection probabilities}

\item{dformula}{formula for delta dependence function}

\item{indep}{if TRUE, uses full independence model}

\item{PI}{if TRUE, uses point independence model}

\item{debug}{plots p1, conditional p1 and delta values}

\item{use.offset}{if TRUE use offset value for logit}
}
\value{
simulation observation dataframe;The return value is the dataframe of the observations seen by at least one observer and the field detected is added to indicate whether the observer detected the observation.  Those missed by both observers are not included.
}
\description{
The return value is the dataframe of the observations seen by at least
one observer and the field detected is added to indicate whether the observer
detected the observation.  Those missed by both observers are not included.
}
\examples{
N=1000
W=10
x=sim.bpi(x=data.frame(observer=rep(c(1,2),times=N),object=rep(1:N,each=2),
 distance=rep(runif(N/2,0,W),each=2)),
 par=c( 1.517424, 1.663175, -0.28, -0.5, 0.1311178),
 pformula=~observer*distance,dformula=~-1+distance,PI=TRUE)

}
